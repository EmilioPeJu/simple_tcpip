fixture = static_library('fixture', 'fixture.c', dependencies : inproject_deps)
fixture_dep = declare_dependency(link_with : fixture,
                                 include_directories :
                                  include_directories(['.']))
